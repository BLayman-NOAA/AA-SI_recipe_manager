# HB1603 UC1

The UC1 recipes live in `AA-SI_Full_Pipeline_Example/example_recipes/HB1603/UC1`
and the structure tests in `tests/test_dive_profiles_recipes.py` read them from
there. A copy kept here had drifted behind the working set and was removed on
2026-09-16.

`Auxiliary/` holds the dive configs and Echoview lines the recipes need.
`outputs/` and `recipe_cache/` are local artifacts of an earlier smoke run.
